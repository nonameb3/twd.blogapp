<% include ./partials/header %>
<h>Index page</h>

<% blogs.forEach((blog)=>{%>

    <div>
        <h1><%=blog.title%></h1>
        <img src="<%=blog.image%>">
        <span><%=blog.createDate%></span>
        <p><%=blog.body%></p>
    </div>
    
<% }) %>
<% include ./partials/footer %>